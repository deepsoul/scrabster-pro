{
  "name": "scrabster-pro",
  "version": "1.0.0",
  "description": "Scrabster Pro - das lustige Buchstaben-Salat Spiel",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "node backend/server.js",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "install:all": "npm install && cd frontend && npm install",
    "test": "node backend/tests/testLetterGenerator.js && node backend/tests/testDictionary.js && node backend/tests/testGameManager.js",
    "vercel-build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ws": "^8.14.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "concurrently": "^8.2.1"
  }
}
